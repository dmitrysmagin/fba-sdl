#include <GL/gl.h>
#include <GL/glu.h>
#include <GL/glext.h>


#include "burner.h"
#include "glframe.h"

//#define frame_timer
#ifdef frame_timer
#include <sys/time.h>
#endif

glframe::glframe(wxWindow* parent, wxWindowID id, const wxPoint& pos, const wxSize& size, long style, const wxString& name, int* attribList) : wxGLCanvas (parent, id, pos, size, style, name, attribList)
{

	SetCurrent();


	SwapBuffers();
}


//This will draw a frame. Some things to consider:
// 1. This relies on the video plugin to manage texture and gamescreen.
void glframe::Draw()
{
	if(bDrvOkay)
	{

}

void glframe::Resize(const wxSize& size)
{

}

void glframe::OnIdle(wxIdleEvent& event)	
{
	Draw();
	event.RequestMore();
}


BEGIN_EVENT_TABLE(glframe, wxGLCanvas)
    EVT_IDLE(glframe::OnIdle)
END_EVENT_TABLE()
