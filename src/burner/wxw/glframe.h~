#ifndef __GLFRAME_H                // Make sure to only declare these classes once
#define __GLFRAME_H

class glframe: public wxGLCanvas 
{
     public:
	glframe(wxWindow* parent, wxWindowID id, const wxPoint& pos, const wxSize& size, long style, const wxString& name, int* attribList);

	void Resize(const wxSize& size);
     private:
	void Draw();
	void OnIdle(wxIdleEvent& event);

	DECLARE_EVENT_TABLE();
};



#endif
